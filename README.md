# hard-job-easy-job
Agentic Job Search Tool — A multi-agent LangGraph pipeline that scrapes job postings, scores them against your profile using LLMs, generates tailored resumes, and automates applications via Playwright — with human-in-the-loop review and a local CLI dashboard.
